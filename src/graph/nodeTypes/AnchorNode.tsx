// Anchor nodes for each corner of the canvas,
// to allow viewing of the whole minimap.
export function AnchorNode() {
  return (<div style={{width: 1, height: 1, opacity: 0, pointerEvents: 'none'}}/>)
}
